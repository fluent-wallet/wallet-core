{
  "name": "wallet-core",
  "private": true,
  "scripts": {
    "build:core": "turbo build --filter=@cfx-kit/wallet-core-methods",
    "dev:extensions": "pnpm run dev --filter=extensions-example",
    "dev:react": "turbo run dev --filter=react-example --filter=common-web",
    "dev:vue": "turbo run dev --filter=vue-example",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.1"
  },
  "packageManager": "pnpm@9.9.0",
  "engines": {
    "node": ">=20"
  }
}
